int singleNumber(int* nums, int numsSize) {
    if (nums == NULL) {
        return 0;
    }
    //if (numsSize == 1) {
    //    return nums[0];
    //}
    //不要写无用分支
    int res = 0;
    for(int i = 0; i < numsSize; i++){
        res = res ^ nums[i];
    }
    return res;
}