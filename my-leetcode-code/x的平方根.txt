int mySqrt(int x) {
    int i = 0;
    int num = x;
    long transmit = x;
    while(transmit*transmit > x){
        transmit = (transmit + num / transmit)/2;
        i++;
    }
    return (int)transmit;
}