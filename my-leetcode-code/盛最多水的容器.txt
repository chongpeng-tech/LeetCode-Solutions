int min(int a,int b){
    if(a>=b){
        return b;
    }else{
        return a;
    }
}
int maxArea(int* height, int heightSize) {
   int front = 0;
   int rear = heightSize-1; 
   int maxrealV= min(height[front],height[rear])*(rear-front);
   int tempRealV;
    //尝试非暴力破解
 


    while(front<rear){
        if(height[rear]>=height[front]){
            front++;
            tempRealV = min(height[front],height[rear])*(rear-front);
            if(tempRealV>maxrealV){
                maxrealV = tempRealV;
            }
        }else{
            rear--;
            tempRealV = min(height[front],height[rear])*(rear-front);
            if(tempRealV>maxrealV){
                maxrealV = tempRealV;
            } 
        }
        if(front==rear){
            break;
        }
    }
    //双循环超时！
    /*
    for(int i = heightSize-1;i>0;i--){
        for(int j = 0;j<heightSize-1;j++){
            if(j>=i){
                break;
            }
            int smallerWallHeight;
            if(height[i]>=height[j]){
                smallerWallHeight = height[j];
            }else{
                smallerWallHeight = height[i];
            }
            tempRealV = smallerWallHeight*(i-j);//本步计算出了问题，木桶原理！！
            if(maxrealV<tempRealV){
                
                maxrealV = tempRealV;
            }
        }
    }
    */
    return maxrealV;
}